export default {
    async createUser(requeste, response){
        // Código para criar um usuário no banco de dados 
        try {
            
            
        } catch (error) {
            return response.json({ message: error.message })
        }
    }
}